# file-length ratchet baseline — paths already over the cap, pinned to
# their current line count. They may SHRINK but not GROW; new files must
# stay <= 1500. Regenerate with: python scripts/check_file_length.py --write-baseline
#
# EMPTY as of ADR-060 (Phase 2.2 complete): every source file is now <= 1500 lines.
# No baselined exceptions remain — the ratchet is a hard 1500 cap for all files.
